# =============================================================
#  .env  (place in repo root next to docker-compose.yml)
#
#  This file stores secrets that docker-compose.yml references
#  using the ${VARIABLE_NAME} syntax.
#
#  IMPORTANT: Add .env to your .gitignore so it is NEVER
#  committed to GitHub. Each developer keeps their own copy.
# =============================================================

# Your ExchangeRate-API key
EXCHANGE_RATE_API_KEY=172048bcea608daa65b4a2cb

# SQL Server SA password (must match docker-compose.yml)
SA_PASSWORD=GLMS_Docker_Pass1!
